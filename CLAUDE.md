@AGENTS.md

# Claude Code

This file intentionally contains no rules. All rules and project facts live in
`AGENTS.md` (imported above) so every AI coding platform gets the same
experience. Change `AGENTS.md`, not this file. Also read `AGENTS.local.md` (git-ignored,
machine-specific) at the start of any session that touches deployment, if it exists.
