@./AGENTS.md

# Gemini CLI

This file intentionally contains no rules. Follow `AGENTS.md` (imported above);
change that file, not this one. Also read `AGENTS.local.md` (git-ignored,
machine-specific) at the start of any session that touches deployment, if it exists.
